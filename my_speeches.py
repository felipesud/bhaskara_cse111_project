import requests
from tkinter import *





















screen = Tk()
screen.title('Caçapava Ward Speeches invitation')
orientation_text = Label(screen, text='Speech Invite')
orientation_text.grid(column=0, row=0)

screen.mainloop()